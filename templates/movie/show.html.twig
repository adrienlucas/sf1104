{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Movie detail{% endblock %}

{% block body %}
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{ movie.title }}</h5>
            <h6 class="card-subtitle mb-2 text-muted">{{ movie.release_date|date('F Y') }}</h6>
            <p class="card-text">{{ movie.description }}</p>
        </div>
    </div>
{% endblock %}
